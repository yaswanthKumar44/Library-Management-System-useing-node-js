<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Member</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        /* Basic Styling */
        body {
            font-family: 'Arial', sans-serif;
            background: linear-gradient(135deg, #c9d6ff, #e2e2e2);
            margin: 0;
            padding: 20px;
            text-align: center;
            transition: all 0.3s ease;
        }

        header {
            background: #007BFF;
            color: white;
            padding: 15px 0;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            transition: background 0.3s ease;
        }
        
        header:hover {
            background: #0056b3;
        }

        header nav ul {
            list-style-type: none;
            padding: 0;
            display: flex;
            justify-content: center;
            gap: 20px;
        }

        header nav ul li a {
            color: white;
            text-decoration: none;
            font-size: 18px;
            transition: transform 0.3s ease, color 0.3s ease;
        }

        header nav ul li a:hover {
            color: #ffdf00;
            transform: scale(1.1);
        }

        /* Form Container */
        .edit-form-container {
            background-color: #fff;
            border-radius: 10px;
            padding: 20px;
            width: 80%;
            margin: 20px auto;
            text-align: left;
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.3);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .edit-form-container:hover {
            transform: scale(1.03);
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.4);
        }

        /* Form Header */
        .edit-form-container h2 {
            text-align: center;
            font-size: 24px;
            color: #333;
            margin-bottom: 20px;
            position: relative;
        }

        .edit-form-container h2::after {
            content: "";
            width: 50px;
            height: 3px;
            background-color: #007BFF;
            position: absolute;
            bottom: -10px;
            left: 50%;
            transform: translateX(-50%);
        }

        /* Form Fields */
        .form-group {
            margin: 20px 0;
        }

        .form-group label {
            display: block;
            font-weight: bold;
            margin-bottom: 5px;
            color: #555;
            font-size: 16px;
        }

        .form-group input,
        .form-group select {
            width: 100%;
            padding: 10px;
            border: 1px solid #ccc;
            border-radius: 5px;
            font-size: 16px;
            transition: border-color 0.3s ease, box-shadow 0.3s ease;
        }

        .form-group input:focus,
        .form-group select:focus {
            border-color: #007BFF;
            box-shadow: 0 0 8px rgba(0, 123, 255, 0.5);
            outline: none;
        }

        /* Button Styling */
        .submit-btn {
            display: block;
            width: 100%;
            padding: 12px;
            background-color: #28a745;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 18px;
            transition: background-color 0.3s ease, transform 0.3s ease;
        }

        .submit-btn:hover {
            background-color: #218838;
            transform: translateY(-2px);
        }

        /* Profile Photo Styling */
        .profile-photo {
            display: flex;
            align-items: center;
            gap: 15px;
            margin-top: 15px;
        }

        .profile-photo img {
            border-radius: 50%;
            border: 2px solid #007BFF;
            transition: transform 0.3s ease;
        }

        .profile-photo img:hover {
            transform: scale(1.1);
        }

        /* Responsive Adjustments */
        @media (max-width: 768px) {
            .edit-form-container {
                width: 90%;
            }

            header nav ul {
                flex-direction: column;
                gap: 10px;
            }
        }
    </style>
</head>
<body>
    <header>
        <nav>
            <ul>
                <li><a href="/librarian-home"><i class="fas fa-house"></i> Dashboard</a></li>
                <li><a href="/logout" id="logout-link">Logout</a></li>
            </ul>
        </nav>
    </header>

    <div class="edit-form-container">
        <h2>Edit Member Details</h2>
        <form action="/update-member/<%= member.username %>" method="POST" enctype="multipart/form-data">
            <div class="form-group">
                <label for="name">Name:</label>
                <input type="text" id="name" name="name" value="<%= member.name %>" required>
            </div>

            <div class="form-group">
                <label for="type">Type:</label>
                <select id="type" name="type" required>
                    <option value="Student" <%= member.type === 'Student' ? 'selected' : '' %>>Student</option>
                    <option value="Faculty" <%= member.type === 'Faculty' ? 'selected' : '' %>>Faculty</option>
                </select>
            </div>

            <div class="form-group">
                <label for="gmail">Gmail:</label>
                <input type="email" id="gmail" name="gmail" value="<%= member.gmail %>" required>
            </div>

            <div class="form-group">
                <label for="mobile">Mobile Number:</label>
                <input type="text" id="mobile" name="mobile" value="<%= member.mobile %>" required>
            </div>

            <div class="form-group">
                <label for="address">Address:</label>
                <input type="text" id="address" name="address" value="<%= member.address %>" required>
            </div>

            <div class="form-group">
                <label for="password">Password:</label>
                <input type="password" id="password" name="password" value="<%= member.password %>" required>
            </div>

            <div class="form-group">
                <label for="branch">Branch:</label>
                <select id="branch" name="branch" required>
                    <option value="CSE" <%= member.branch === 'CSE' ? 'selected' : '' %>>CSE</option>
                    <option value="CSE AIML" <%= member.branch === 'CSE AIML' ? 'selected' : '' %>>CSE AIML</option>
                    <option value="AIDS" <%= member.branch === 'AIDS' ? 'selected' : '' %>>AIDS</option>
                    <option value="IT" <%= member.branch === 'IT' ? 'selected' : '' %>>IT</option>
                    <option value="EEE" <%= member.branch === 'EEE' ? 'selected' : '' %>>EEE</option>
                    <option value="ECE" <%= member.branch === 'ECE' ? 'selected' : '' %>>ECE</option>
                    <option value="Mechanical" <%= member.branch === 'Mechanical' ? 'selected' : '' %>>Mechanical</option>
                    <option value="Aerospace" <%= member.branch === 'Aerospace' ? 'selected' : '' %>>Aerospace</option>
                    <option value="Civil" <%= member.branch === 'Civil' ? 'selected' : '' %>>Civil</option>
                </select>
            </div>
            

            <div class="form-group">
                <label for="profilePhoto">Profile Photo:</label>
                <div class="profile-photo">
                    <% if (member.profilePhoto) { %>
                        <img src="/uploads/<%= member.profilePhoto %>" alt="Profile Photo" width="100">
                    <% } %>
                    <input type="file" id="profilePhoto" name="profilePhoto">
                </div>
            </div>

            <button type="submit" class="submit-btn">Update Member</button>
        </form>
    </div>
</body>
</html>
